import User from '../models/User.js';
// import bcrypt from 'bcryptjs';
import jwt from 'jsonwebtoken';
const { JWT_SECRET } = process.env;

const resolvers = {
  // Resolver functions here
};

export default resolvers;